# ngt039
1) Install the required dependencies by running these commands in your terminal

Command	Description

   pip install fastapi
   pip install uvicorn	

2. Start your application by running the command

  uvicorn api_ngt039_501:app --reload

makesure run at right folder
 ➜ /workspaces/ngt039/ngt039 (main) $ uvicorn api_ngt039_501:app --reload

3. Plese note that only some variable in code  is temporary assign due to no db connect
  
